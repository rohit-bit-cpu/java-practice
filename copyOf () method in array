import java.util.Scanner;
import java.util.Arrays;

class A{
    public static void main(String[] args){
       int temp;
        int a[]= new int[5];
        
        System.out.println("Enter data in array1:");
        Scanner obj = new Scanner(System.in);  // to input value in run time we use scanner class.
        
        for(int i= 0;i<a.length; i++){
            a[i]=obj.nextInt();    
        }
         int a2[] = Arrays.copyOf(a,5);
        System.out.println(" data in array2 :" );
        for(int i = 0;i<a.length;i++){
           System.out.println(a2[i]+" ");
        }
       
         
    }
}

size of both array should be same, otherwise it will show error IndexOutOfBoundsException ..
